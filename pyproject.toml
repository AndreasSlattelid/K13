[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/k13"]

[project]
name = "k13"
version = "0.0.1"
dependencies = [
    "numpy~=1.23.3",
    "scipy~=1.9.1"
]